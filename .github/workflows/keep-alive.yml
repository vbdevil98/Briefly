name: Keep Render Service Alive

on:
  schedule:
    # Runs every 5 minutes
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the website
        run: curl -s "https://brieflyai.in/" > /dev/null
